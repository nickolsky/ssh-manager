bin/
obj/
.vs/
*.user
# Personal encrypted vault, settings and known_hosts - never commit
data/
# Published build and the side-by-side test build (publish-dev.ps1) with its copy of the data
app/
app-dev/
data-*/

tools/codemirror/node_modules/
